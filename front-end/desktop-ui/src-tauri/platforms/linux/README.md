# Linux-specific Tauri (front-end shell) files

Place Linux-only Tauri shell artifacts here: custom `tauri.conf.json`
overrides, Linux build scripts, or packaging files (`.deb`, AppImage
hooks). Shared, cross-platform Tauri code lives in the parent `src-tauri/`.
