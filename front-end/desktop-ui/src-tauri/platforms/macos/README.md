# macOS-specific Tauri (front-end shell) files

Place macOS-only Tauri shell artifacts here: `tauri.conf.json` overrides,
code-signing / notarization scripts, or `.app` packaging hooks. Shared,
cross-platform Tauri code lives in the parent `src-tauri/`.
