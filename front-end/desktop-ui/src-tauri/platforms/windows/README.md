# Windows-specific Tauri (front-end shell) files

Place Windows-only Tauri shell artifacts here: `tauri.conf.json` overrides,
NSIS/MSI installer scripts, or signing configuration. Shared,
cross-platform Tauri code lives in the parent `src-tauri/`.
