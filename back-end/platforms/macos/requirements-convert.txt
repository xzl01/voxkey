# SPDX-License-Identifier: MIT
#
# Dependencies for the LOCAL FunASR -> Core ML conversion fallback
# (convert_funasr_coreml.py). Only needed when the pre-converted package
# download is unavailable; the normal install path uses the downloaded
# package and does NOT need these.
#
# Install into the same venv after requirements.txt:
#   pip install -r back-end/platforms/macos/requirements-convert.txt
funasr>=1.0
modelscope>=1.15
torch>=2.1
torchaudio>=2.1
onnxscript>=0.1
sentencepiece>=0.1.99
