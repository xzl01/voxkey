line-length = 100
target-version = "py310"

[lint]
# Keep the baseline strict-but-practical: syntax/undefined-name/import errors
# (E, F) are enforced; style nits like line length are not, to avoid churn.
select = ["E", "F"]
ignore = ["E501"]
